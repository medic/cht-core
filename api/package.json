{
  "name": "medic-api",
  "description": "Provides a server side API.",
  "private": true,
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/medic/cht-core.git"
  },
  "engines": {
    "node": ">=22.11.0",
    "npm": ">=10.9.0"
  },
  "scripts": {
    "toc": "doctoc --github --maxlevel 2 README.md",
    "run-watch": "TZ=UTC nodemon -e js,json,html,properties --inspect=0.0.0.0:9229 --ignore 'build/static' --ignore 'build/public' --watch ./ --watch '../shared-libs/**/src/**' server.js -- --allow-cors"
  }
}
