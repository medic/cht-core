//var helper = require('../../helper'),
   // faker = require('faker');

    
   var pageTitle = 'Medic Mobile';
   var noMessageErrorField = $('[ng-show="!error && !loading && !reports.length"]');
    
   var rightHandPane= $('[ng-show="!selected.reports[0] && !loadingContent"]');
    
    //functions to interact with our page
