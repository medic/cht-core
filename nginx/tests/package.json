{
  "name": "nginx-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test:unit": "npx mocha unit/*.spec.js --no-config",
    "test:integration": "npx mocha integration/*.spec.js --no-config"
  }
}
