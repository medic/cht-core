{
  "name": "nginx-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"running tests\" && npx mocha *.spec.js --no-config"
  },
  "dependencies": {
    "chai": "^4.3.8",
    "mocha": "^11.1.0",
    "sinon": "^18.0.1"
  }
}
