{
    "name": "settings",
    "version": "0.0.12",
    "categories": ["build-tools"],
    "description": "Uploads the kanso.json file as a CommonJS module under the 'settings' directory. Useful for packages that need access to these values after deployment.",
    "preprocessors": {
        "add": "build/add"
    },
    "postprocessors": {
        "root": "build/add_root"
    }
}
