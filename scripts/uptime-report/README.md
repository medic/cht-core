# Uptime report

Queries the Uptime Robot API and reports on the uptime for the last 30 days for each of the configured monitors.

To run:

1. Define a `.env` file with `API_KEY = <your api key>`
2. Execute `node index.js`
